version: '3.5'

services:
  duob-tms-web:
    image: 127.0.0.1:5000/duob-tms-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:80"   # container runs nginx on 80, mapped to host 8081
    restart: always