import {ADMIN, ENGINEER, MANAGER} from "./roles";

const WAREHOUSE_STATE = {
    LIST: [
        ADMIN,
        ENGINEER,
        MANAGER
    ],
    SELECT_WAREHOUSE: [
        ADMIN
    ]
}
export default WAREHOUSE_STATE
