import {ADMIN} from "./roles";

const EMPLOYEE = {
    LIST: [
        ADMIN
    ],
    CREATE: [
        ADMIN
    ],
    EDIT: [
        ADMIN
    ],
    DELETE: [
        ADMIN
    ]
}

export default EMPLOYEE;
